const products = ['aphogee', "l'oreal", 'mac', 'blanc', 'nude'];

const productProps = {
    
}

const saveProduct = () => {
    const savedProducts = products.map(product => {
        product
    });
};